l = []

while True:

    l.append(int(input('digite os valores da lista')))

    r = str(input('Deseja continuar: [S/N]'))
    
    if r in 'Nn':

        break

print (l)

soma=0

for i in l:

    soma+=i

print(soma)
